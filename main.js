// Lógica auxiliar de registro de ventas por vendedor y cantidad de cartones
// (La lógica principal y UI reactiva se encuentra integrada dinámicamente en index.html)

document.addEventListener('DOMContentLoaded', () => {
    const form = document.getElementById('bingoForm');
    if (!form) return;

    // Listener backup si fuera necesario
    console.log("Sistema de Gestión de Bingo (Vendedor / Cantidad) Inicializado.");
});